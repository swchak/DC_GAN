# DC_GAN

Deep Convolutional GAN (DCGAN) experiments implemented in PyTorch using Jupyter notebooks. This repository explores generative modeling across multiple datasets, including handwritten characters, fashion items, flowers, and pet images.

## Project Summary

This project focuses on training **Generative Adversarial Networks (GANs)** and **Deep Convolutional GANs (DCGANs)** to generate grayscale image samples from different domains. Each notebook contains a self-contained experiment for loading data, defining the generator and discriminator, training the model, and visualizing generated outputs.

The goal of this repository is to explore how GAN-based architectures behave across datasets with different visual structure and complexity.

## Datasets Explored

This repository includes experiments on:

- **EMNIST Letters** — handwritten character generation
- **Fashion-MNIST** — grayscale clothing image generation
- **Flowers102** — grayscale flower image generation
- **Oxford-IIIT Pet** — grayscale pet image generation

## Repository Contents

- `EMNIST.ipynb`  
  GAN/DCGAN training on the EMNIST letters dataset.

- `FMNIST_DC_GAN.ipynb`  
  DCGAN experiment on Fashion-MNIST.

- `PETS_DC_GAN.ipynb`  
  DCGAN experiment on the Oxford-IIIT Pet dataset.

- `myDC_GAN.ipynb`  
  Custom DCGAN experiment using the Flowers102 dataset.

- `myGAN.Ipynb`  
  Additional GAN experimentation notebook.

- `utils.py`  
  Helper utilities for dataset loading and image grid visualization.

## What This Project Demonstrates

- Building GAN/DCGAN architectures in **PyTorch**
- Training generator and discriminator networks adversarially
- Preparing image datasets using **Torchvision**
- Visualizing model outputs during training
- Comparing generative performance across multiple datasets

## Tech Stack

- **Python**
- **PyTorch**
- **Torchvision**
- **NumPy**
- **Matplotlib**
- **Jupyter Notebook**

## Model Overview

The notebooks define two key neural networks:

### Generator
The generator takes a random latent noise vector and transforms it into a synthetic image using transposed convolution layers, batch normalization, and nonlinear activation functions.

### Discriminator
The discriminator receives either a real image from the dataset or a fake image generated by the generator and learns to classify whether the input is real or fake.

Together, these networks are trained in an adversarial setting where:

- the **generator** learns to produce realistic images
- the **discriminator** learns to distinguish generated images from real ones

## Installation

Clone the repository:

```bash
git clone https://github.com/swchak/DC_GAN.git
cd DC_GAN
```

Install dependencies:

```bash
pip install torch torchvision matplotlib numpy jupyter
```

## Running the Project

Start Jupyter Notebook:

```bash
jupyter notebook
```

Open any notebook and run the cells in sequence. Recommended starting points:

- `FMNIST_DC_GAN.ipynb`
- `EMNIST.ipynb`
- `PETS_DC_GAN.ipynb`

## Typical Workflow

Each notebook generally follows this pipeline:

1. import required libraries
2. load and preprocess the dataset
3. define generator and discriminator architectures
4. initialize model weights
5. configure loss function and optimizers
6. train both networks
7. visualize generated samples over time

## Utilities

The repository includes helper functionality in `utils.py` for:

- loading supported datasets into PyTorch dataloaders
- displaying batches of images as grids for inspection

This helps keep notebook experiments cleaner and easier to reuse.

## Learning Goals

This project is useful for understanding:

- the fundamentals of GAN training
- DCGAN architecture design
- the effect of dataset type on generated image quality
- the challenges of adversarial optimization

## Possible Future Improvements

- add a `requirements.txt` file
- save generated sample outputs to an `assets/` folder
- include training loss plots for each dataset
- document hyperparameter choices per notebook
- add support for RGB image generation
- refactor notebook code into reusable training scripts

## License

This repository does not currently include a license file.

## Author

Created by **swchak**
